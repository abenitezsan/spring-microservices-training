spring:
  application:
    name: spring-boot-admin
  cloud:
    config:
      uri: http://localhost:8001
      username: user
      password: microservicesTraining
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER