spring:
  application:
    name: spring-turbine
  cloud:
    config:
      uri: http://localhost:8001
      username: user
      password: microservicesTraining
      discovery:
        enabled: true
        serviceId: CONFIG-SERVER